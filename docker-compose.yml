services:
  transcriber:
    build: .
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
    environment:
      - TRANSCRIBE_DEFAULT_MODEL=small
      - TRANSCRIBE_TMP_DIR=/tmp/transcriber
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
